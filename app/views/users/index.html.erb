<% provide :title, "All users" %>
<h1>All users</h1>

<%= paginate(@users) %>

<ul class="users">
 <% @users.each do |user| %>
  <li>
    <%= render @users %>
  </li>
 <% end %>
</ul>

<%= paginate(@users) %>
